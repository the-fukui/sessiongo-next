{
  "name": "hasura",
  "packageManager": "yarn@3.0.2",
  "scripts": {
    "hasura": "docker compose up && docker compose down",
    "dev": "npx hasura console --envfile .env.development"
  },
  "devDependencies": {
    "hasura-cli": "^2.0.8"
  }
}